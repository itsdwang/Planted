import 'package:flutter/material.dart';

class PlantsPage extends StatelessWidget {
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Login to Planted!"),
        ),
        body: Text("Plants Page"));
  }
}
